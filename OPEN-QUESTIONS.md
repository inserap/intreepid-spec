# Questions ouvertes — spec

> Liste vivante des items non résolus ou différés pour le projet `intreepid`. Les questions touchant la doctrine partagée (`standards/`) sont résolues ici, même si elles produisent un commit doctrinal côté standards.
> Mise à jour pendant les rituels session-end (bins c. additions, d. résolutions).
> Les IDs `Q-NNNN` sont **immuables** : ne jamais réutiliser un Q-NNNN même après résolution.

## Comment utiliser ce fichier

- **Ajouter** un item quand une session fait émerger une vraie ambiguïté non résolvable en séance.
- **Résoudre** un item quand une session livre une réponse (ADR, entrée journal, slice execution).
- **Raffiner** un item quand sa formulation évolue.

## Ouvertes

| ID | Question | Apparue | Catégorie | Notes |
|---|---|---|---|---|
| Q-0001 | Nom définitif du projet ? | 2026-07-26 | Produit / naming | Nom de code actuel : `intreepid`. Candidats pressentis : *Semantree*, *Semantrek*. Anciens candidats (Ramure, Sylva, Dendrite, Arolle) abandonnés. Critère : raconter l'arbre *et* l'enracinement territorial/sémantique. |
| Q-0002 | Dataset pilote et question métier réelle pour la v1 ? | 2026-07-26 | Produit / périmètre | Cas accidents (OFROU, open data, couches geo.admin.ch) pressenti mais non confirmé ; aucun destinataire métier identifié à ce jour. **Raffinement (cadrage brique #5, 2026-08-01)** : la brique #5 est cadrée comme **banc de validation de la v1 sur la vraie donnée OFROU** — substitut *contrôlable* à cette question en attendant un vrai destinataire ; elle **ne la résout pas** (ni question métier réelle ni destinataire), mais exerce le système de bout en bout sur du réel. **Validée (brique #5 impl, v0.6.0, 2026-08-01)** : la validation end-to-end sur le réel a **réussi** — posture P6 confirmée sur donnée propre (aucune anomalie hallucinée), volume≠excès démontré (BE sur-concentré / ZH gros comptage expliqué par l'exposition), refus causal motivé, run $0,19. **Q-0002 reste ouverte** : substitut contrôlé, pas un vrai destinataire métier. |
| Q-0003 | Granularité des nœuds de l'arbre : quels critères pour que le greffier décide ce qui mérite un nœud ? | 2026-07-26 | Technique | Pari technique central du projet. À calibrer sur sessions réelles, pas sur le papier. **Raffinement (recherche greffier, 2026-07-30)** : la littérature fournit des heuristiques publiées (H1–H8, cf. `research/2026-07-30-greffier-provenance.md` §4) — grain-cible = **Action sémantique** (Gotz & Zhou 2009 : ni notes manuelles ni events bruts) ; un nœud naît à (a) résultat interprétable d'une Action ou (b) changement d'hypothèse/sujet ; Events bruts conservés mais **repliés** sous l'Action (fin en stockage, agrégé en lecture) ; coalescence des actions consécutives même-artefact ; vocabulaire de `kind` **petit et fermé** (le texte-libre optionnel échoue — Goyal CHI 2013) ; indexation **par artefact, pas par chronologie** (Verdant ; Kery CHI 2018, empirique). Décision brique #4 : capture au **grain événement** sans tri à chaud ; la projection « Action » est une **lecture future** — la calibration sur session réelle reste l'issue de cette question. **Capture livrée (2026-07-31, v0.5.0)** : le grain événement est en production (thinking summarized inclus — 2 nœuds 💭 observés sur le run de démo) ; la calibration « Action sémantique » attend une vraie session multi-tours (Q-0002). |
| Q-0004 | Schéma du catalogue sémantique et modèle de données de l'arbre/trace (avec attribution et empreintes spatiales) ? | 2026-07-26 | Architecture | Prochaine étape n°3 de l'architecture — conditionne tout le reste. **Raffinements (revue externe 2026-07-26)** : (a) en faire le *premier contrat exécutable* (schéma YAML catalogue + modèle d'événement de trace) avant tout code ; (b) empreinte de reproductibilité d'un insight = hash dataset + version catalogue + SQL + params + versions DuckDB/extensions + modèle + version prompt/charte (le « SQL rejouable » seul ne suffit pas si Parquet/catalogue évoluent) ; (c) invariants géospatiaux du dataset, pas champs descriptifs : SRID (EPSG:2056), reprojection, unités de distance, validité géométrique, version H3 ; (d) contrat MCP read-only à durcir en invariants *testés* : bornes sur sorties + quotas temps/mémoire + whitelist datasets/relations + audit par appel + interdiction écritures indirectes (COPY/INSTALL/extensions). **Raffinement biographie (session 2026-07-26)** : distinguer dans le modèle deux natures de résultat — *fait intrinsèque à la donnée* (vrai quelle que soit la question → promu à la **biographie/fiche**, canal **cross-analyse** partagé) vs *insight sur le monde* (spécifique à une analyse → **graphe de connaissances**, canal par-analyse). La fiche est le confluent où les faits-donnée découverts par **n'importe quelle** analyse s'accumulent (ex. une quirk de la couche routes trouvée via l'analyse accidents sert à l'analyse entretien). Le schéma doit prévoir : (i) un champ de faits-donnée promus avec **provenance** (session + requête, P4), (ii) le critère de promotion « vrai et utile quelle que soit la question », (iii) la promotion **filtrée** curateur→MR humain (garde anti-gonflement de la fiche, cousin de Q-0003 / critique NotebookLM n°3), (iv) rappel : une découverte fortuite reste hypothèse jusqu'à survie au critique/modèle nul (Q-0009) avant promotion. **Raffinement frontière charte↔fiche (brique #1, 2026-07-28)** : à graver — la *posture* (honnêteté fait/hypothèse/refusé, jamais de lignes brutes) reste **générique** dans la charte des agents ; la *connaissance per-dataset* (pièges, sens des colonnes, codes sentinelles) vit dans la **fiche**, lue via `describe` au runtime. C'est ce partage qui permet « traiter n'importe quelles données » sans gonfler le prompt. La charte de la brique #1 porte du **surajustement de scénario** (exemple `999`, instruction « concentration → hypothèse » tunée pour l'oracle) → à extraire vers la fiche / des exemples neutres lors de la généralisation **sur un 2ᵉ dataset réel** (ne se fera pas tout seul — dette à solder délibérément). **Recherche + revue 2026-07-29** : le split *Skills = flux cognitif ÷ MCP = calcul* de `kevics1/geo-perception` est la même frontière charte↔fiche sous un autre nom ; le *contexte-extension YAML* de `stuzero/pg-mcp-server` et la *clé BFS* de `malkreide/swisstopo-mcp` sont des patrons concrets pour la fiche `describe` ; la revue spec↔code a re-confirmé que `agent/charter.md` porte encore les exemples tunés (`999`, concentration→hypothèse). Cf. `research/2026-07-29-etat-de-lart-github.md`. **Note (2026-07-30)** : les responsabilités de *curation de la fiche* (inférence de type, sensibilité/k-anonymat, hygiène anti-spoiler) sont **extraites en Q-0015** pour ne pas surcharger cette question ; Q-0004 reste focalisée sur le **schéma** catalogue/fiche + modèle de trace. *(Grooming à prévoir : sortir aussi les fils « biographie/fait-vs-insight » et « frontière charte↔fiche » vers des questions dédiées — cf. follow-up journal 2026-07-30.)* **Raffinement modèle de trace (brique #4, 2026-07-30)** : premier **contrat exécutable** posé au design du greffier — tables DuckDB immuables `sessions` (statuts `open/closed/aborted`) + `nodes(kind, parent_id, content/meta JSON)`, append-only, scellées à la clôture ; nommage des relations en vocabulaire **PROV-DM allégé** (verdict recherche : emprunter Entity/Activity/Agent + `wasInformedBy`/`wasDerivedFrom`/`wasAttributedTo`, **écarter** la stack RDF/PROV-O/PROV-JSON — RETEX : « PROV a gagné les specs, perdu les SDK » ; conversion a posteriori via `trungdong/prov` si un consommateur externe l'exige) ; extension pressentie par **facets** (patron OpenLineage `_producer`/`_schemaURL`, schémas côté fiche — cohérent frontière charte↔fiche). Ne clôt pas Q-0004 (mono-analyste quasi-linéaire ; DAG multi-parent, artefacts versionnés façon Verdant et facets outillés = extensions futures). **Corrections à la veille 2026-07-29** : PROV-AGENT a bien un repo vérifiable (`ORNL/flowcept`, MIT, actif) ; les conventions OTel GenAI ont migré vers `open-telemetry/semantic-conventions-genai`. Cf. `research/2026-07-30-greffier-provenance.md`. **Contrat livré + clarification session≠analyse (brique #4 impl, 2026-07-31, v0.5.0)** : le contrat `sessions`+`nodes` est **en production** (append-only vérifié par tests sur les 3 états `closed`/`aborted`/`open`-crashée). Vocabulaire gravé au Q&A du gate : **session = épisode de capture** (un run de l'analyste) ≠ **analyse = fil multi-sessions** (jours/mois, reprises, branches en attente) ; la reprise d'une analyse se fait **par référence** à l'histoire (`wasInformedBy`/`wasDerivedFrom`, jamais par réouverture d'une session scellée — sémantique git). Le lien inter-sessions est le prochain étage du schéma. V2-list revue finale : nœud `verdict_raw` quand `parse_verdict` échoue (le texte brut non parsé est perdu hors `aborted_reason`) ; `render()` n'affiche pas `SessionTrace.meta` (coûts/num_turns). **Raffinement (cadrage brique #5, 2026-08-01)** : contrat de **fiche auto-descriptive** conçu — la fiche porte `data:` + `exposures` en chemins **relatifs-à-la-fiche**, le serveur MCP est pointé par `INTREEPID_FICHE` (une fiche = point d'entrée unique, `dataset` = nom de table) ; **notebook Quarto** posé comme projection lisible/rejouable de la trace (`to_quarto` pur, templating figé, callouts fait/hypothèse/refusé). Conçus au **palier A**, en **attente d'impl** (design/plan en vol). **Shippé (brique #5, v0.6.0, 2026-08-01)** : `to_quarto` (générateur agnostique, golden-testé) + fiche auto-descriptive (`data:`+`exposures` relatifs, serveur pointé par `INTREEPID_FICHE`) sont **en production**. Restent ouverts : liens inter-sessions (`wasInformedBy`), DAG, distillation, rappel MCP. **Brique #6 (v0.7.0, 2026-08-02)** : `load_referenced_fiche` (résolution d'une fiche d'exposition **référencée** par nom, garde anti-traversée de chemin) **en production** ; convention gravée **clé d'exposition = colonne de jointure du dataset** (`canton` catégoriel, `geom` spatial) ; l'exposition spatiale **référence** la fiche auto-descriptive de la grille (STATPOP), zéro recopie. **Pointeur (contrat d'interrogation, 2026-08-02)** : le fil *fait-vs-insight* de cette question se lit désormais sous la mission gravée en overview v0.3 (§1 « Contrat d'interrogation ») — le *fait brut qualifié* et l'*insight factualisé* sont les deux extrémités d'un même continuum de sortie honnête ; la couche produit qui l'expose est extraite en **Q-0020**. **Raffinement (ADR-0009, 2026-08-02)** : l'**architecture d'exécution** qui portera le fait-vs-insight est posée — orchestrateur générique + **profil de rôle** + historique **application-owned** (record-then-substitute) ; le **modèle de nœud** de la trace est à enrichir (grain **Action** projeté, **acteur** humain/agent, **indexation par artefact** — l'acteur en Phase B, Action/artefact différés). La curation devient un **profil** produisant la **fiche** (faits-donnée) ; le mode « pendant découverte » (§4.3) reste le pont curation↔analyse. **Exécuté (#7a, v0.8.0, 2026-08-02)** : le **socle de capture** du greffier (`scribe/store.py`+`trace.py`) est rendu **agnostique du rôle** — primitive générique `record_nodes(specs)`/`TraceBuilder.custom` (nœuds `(kind, content, meta)`) remplace `record_verdict`/`verdict` ; le schéma « observation » descend dans le profil analyste (`grep verdict|observation` sur le socle = vide). Un rôle futur (curateur) émet ses kinds sans toucher au socle. **Reste à livrer (#7c, couche B)** : le **modèle de nœud enrichi** — `TraceNode.actor` (humain/agent, rangé dans `meta`) + cycle ouvert `open/append/seal` **idempotent par id**. **Nouveau follow-up** : les *renderers* (`render.py`/`notebook.py`) dispatchent encore sur `kind=="observation"` — couplage de **lecture** (pas la capture) ; tâche dédiée « registre de rendu par kind fourni par le profil ». **Trace enrichie (#7c, v0.10.0, 2026-08-04)** : nœud durable `curation_validated` (hash) + `human_turn` portant `actor` dans `meta` (socle agnostique, aucun DDL) ; `write_fiche` (dict opaque). Le **mode découverte** (§4.3) confirmé comme pont : les `exposures` s'y enrichissent (cf. Q-0016). **Trace mesurable (#9, v0.11.0, 2026-08-05)** : nœud **`turn_result`** — la fin de tour cesse d'être une méta de session **écrasée à chaque tour** pour devenir un fait d'exécution **horodaté et durable dès la capture** (durées, coût, tokens, raison de terminaison), agnostique du rôle (`meta` vide). Et `TraceNode.ts` : l'horodatage était **écrit** en base depuis le début mais **jamais relu** — la trace portait donc une information temporelle inexploitable. Asymétrie gravée : `TraceBuilder` reste **pur** (aucune horloge, ids `{sid}#{seq}`), le **store est l'unique autorité du temps**. Conséquence pour la suite : toute mesure est une **projection** de la trace (`scribe/metrics.py`), jamais une capture parallèle. **Trace à deux voix (#8 corrigé, v0.12.0, 2026-08-06)** : nœud **`agent_turn`**, miroir exact de `human_turn` — la trace d'une conversation n'enregistrait **qu'une des deux voix**, le socle écartant les `TextBlock` à dessein (ils sont projetés par le profil, ce qui convient au one-shot mais pas à un rôle conversationnel où `on_result` ne se déclenche qu'à la validation). Socle toujours agnostique : `meta` ne porte que `actor`, aucun DDL. Corrigé aussi : la trace **stockait de l'heure locale en croyant stocker de l'UTC** (colonne `TIMESTAMP` sans fuseau → conversion puis dépouillement) ; on écrit désormais de l'UTC naïf et `load` rattache le fuseau. **Cycle de vie d'une fiche produite, tranché ad hoc** : une fiche issue d'une curation réelle reste **matériel** dans le spec tant qu'aucune analyse ne la consomme ; `catalog/` est réservé aux fiches effectivement consommées (précédent #7c ; la fiche du gate du 06/08 vit dans `research/`). **Le CONTRAT de la fiche n'a jamais existé — constat du 2026-08-07.** La fiche est un dict **opaque** pour Python (décision #5 de #7c, assumée), mais personne n'avait vu que cela laissait sa **structure entièrement indéfinie**, et quatre curations réelles le prouvent : le vocabulaire de colonne est réinventé à chaque run (`sens`/`type`/`pieges` en #7c → `role`/`note` en #8 → `description`/`type_retenu` en #10), et les clés racine sont passées de **12** (#7c : `grain`, `perimetre`, `srid`, `pieges_transversaux`, `points_non_tranches`…) à **10** (#8) puis à **3** (#10 : `columns`, `dataset`, `titre`). **Toute la connaissance transversale a disparu en v0.13.0** — cause identifiée : le § Format de sortie réécrit se lit comme une **liste fermée**. D'où aussi une colonne fantôme `_indicateurs_usagers`, l'agent ayant une note transversale et aucun endroit où la mettre. Sans schéma, il n'existe ni notion de *complet*, ni notion de *trop long*, et aucun consommateur (`describe`) ne peut s'appuyer sur quoi que ce soit. **Le schéma minimal se prescrit dans la CHARTE, pas dans Python** : l'opacité côté transporteur reste préservée. À livrer dans la slice suivante. |
| Q-0005 | Réutilisation concrète du substrat Henry : les Plans/Traits sont-ils repris tels quels, adaptés, ou seulement comme inspiration de format ? | 2026-07-26 | Architecture | ADR-0005 pose l'absorption dans le principe ; le degré reste à trancher au moment du squelette. |
| Q-0006 | Empreintes spatiales floues : seuils de la représentation dégradée (emprise exacte → communes → canton) ? | 2026-07-26 | Technique | À trancher lors de l'implémentation du couplage arbre-carte (v2). |
| Q-0008 | Injection de prompt via colonnes textuelles des données : quelles données du périmètre contiennent du texte tiers, et quel traitement ? | 2026-07-26 | Sécurité | Risque identifié lors de la revue écosystème ; à qualifier avec le dataset pilote. **Piste (revue externe 2026-07-26)** : le modèle « le LLM ne voit pas les lignes » réduit le risque mais valeurs catégorielles, échantillons et métadonnées restent du contenu tiers non fiable. Définir un type `untrusted_data` dans le protocole de rendu des outils MCP : encodage structuré, quotas de taille, neutralisation des instructions, séparation visuelle stricte dans le contexte agent. **Recherche 2026-07-29** : `kyle-gao/SecuringGISAgent` outille le durcissement de prompts + l'évaluation adversariale d'agents géo (rare) ; `diogocosme/geodados-mcp-server` illustre l'**MCP Elicitation** (le serveur *demande* en cas d'ambiguïté) = parade au mode d'échec « résolution silencieuse de l'ambiguïté » mesuré par `hsd2514/geoagenteval`. Cf. veille §3/§5. **Raffinement échantillon-drill-down (brique #2, 2026-07-30)** : P2 autorise explicitement les *échantillons* — donc, sur détection d'anomalie (sentinelle, points hors emprise), tirer un **échantillon borné, pseudonymisé, traité `untrusted_data`** pour comprendre/compléter l'origine est légitime et renforce le curateur ; cadrer les garde-fous (taille bornée, k-anonymat cf. Q-0015, neutralisation d'instructions, read-only, servi comme résultat d'outil et non ingestion de table). |
| Q-0009 | Protocole de preuve statistique spatiale : quels invariants de rigueur imposer avant de retenir un pattern ? | 2026-07-26 | Technique / statistique | Émergé de la revue externe 2026-07-26, complète la mention MAUP + modèles nuls de l'overview. À couvrir : stationnarité, autocorrélation spatiale, découpage temporel, prévention de fuite spatiale (spatial leakage) en validation ML. Se matérialisera dans la charte des agents (critique) et le protocole des modèles nuls. **Recherche 2026-07-29 (outils concrets)** : `pysal/esda` = modèles nuls spatiaux par permutation (`significance.py`) + **test MAUP dédié** (`smaup.py`) ; `SamComber/spacv` = validation croisée spatiale anti-leakage (SLOO) ; `statsmodels.stats.multitest` = corrections multi-tests (garden of forking paths ; BSD vs Pingouin GPL) ; `py-why/dowhy` refuters → outil MCP `refute_insight`. Cf. veille §5. **Brique #3 (2026-07-30) — mécanisme livré** : le premier modèle nul est en production (`concentration_test` : permutation multinomiale conditionnée à l'exposition, pseudo-p `(M+1)/(R+1)`, écart de Poisson standardisé, 2 volets `most_concentrated`/`highest_raw_count`). Restent ouverts, par ordre de valeur pressenti (veille §5) : autocorrélation locale Gi\*/LISA (`esda`, sur maille H3), test MAUP multi-résolution H3 (`esda.Smaup`), méthodes réseau NKDE (biais planaire des accidents routiers), corrections multi-tests FDR (`statsmodels`), E-value (confondeur caché), Empirical Bayes/RTM (régression vers la moyenne), réfutation causale (`dowhy`). Le **choix de l'exposition** (le dénominateur qui conditionne le nul) est extrait en **Q-0016**. **Raffinement (deep-research H3 + brique #6, 2026-08-01)** : la veille `research/2026-08-01-h3-agregation-anti-maup-reseau.md` outille la roadmap (Gi\*/LISA via `esda.G_Local` + null par permutation ; correction multi-tests **FDR obligatoire** — Caldas de Castro & Singer 2006 ; `pygeoda lisa_fdr`/`lisa_bo`) et pose un **verdict planaire** : pour un phénomène **contraint au réseau** (accidents), le binning aréal H3 est biaisé — méthode **réseau** (NKDE, Xie & Yan 2008) plus appropriée, à garder pour une brique future (**hors** brique #6). **Brique #6 (design+plan advisor SHIP, exéc. à venir)** livre le premier volet : robustesse **multi-résolution** d'une concentration (survit-elle au changement de maille H3 ?), null ∝ exposition, garde-fous (autocorrélation sous le null, non-emboîtement ~14 %, cellules non équi-aires). Gi\*/FDR **local** restent différés. **Brique #6 (v0.7.0, 2026-08-02) — volet livré** : `spatial_scale_robustness` en production (agrégation H3 multi-résolution `[6,7,8]`, null ∝ population réelle STATPOP, verdict `robuste`/`fragile`/`absente`, décomposition des cellules **non-peuplées** reportées à part, caveats planaire/non-emboîtement). Smoke réel : verdict `robuste`, part `unpopulated` 4,18 %. Restent différés : Gi\*/LISA local + **FDR**, méthodes **réseau** (NKDE, Xie & Yan 2008 — la méthode « propre » pour un phénomène de réseau), exposition **trafic** réelle. |
| Q-0010 | Modèle d'authentification et de facturation des agents (Claude Agent SDK) : dev vs produit déployé ? | 2026-07-26 | Architecture / contractuel | **Établi** (doc officielle Anthropic) : le Claude Agent SDK s'authentifie par clé API `ANTHROPIC_API_KEY` (ou cloud provider Bedrock/Vertex/Foundry/AWS), **pas** par abonnement ; l'usage de l'abonnement / claude.ai login pour un **produit tiers** est interdit *sauf approbation préalable d'Anthropic* ; un produit déployé relève des Commercial Terms of Service. **Dev / prototypage local par l'auteur = usage interactif permis** → la phase squelette peut tourner sur le compte perso à quota. **Ouvert** : négocier avec l'account team enterprise (a) l'exception « previously approved » pour faire tourner les agents produit sur le plan, (b) le tarif/volume API du backend produit. À élever en ADR quand le modèle de facturation produit se décide. Sources : docs Agent SDK (code.claude.com), Commercial Terms. **Dev résolu en pratique (brique #1, 2026-07-28)** : l'Agent SDK tourne sur l'**abonnement** via l'auth Claude Code stockée (`claude setup-token` → `CLAUDE_CODE_OAUTH_TOKEN`) ; garde runtime dans le runner : refuser de tourner si `ANTHROPIC_API_KEY` est définie (elle masque l'OAuth et facturerait l'API). **Reste ouvert** : facturation du produit déployé. **Fait nouveau (2026-08-06)** : le **Fast mode** (research preview, Opus 4.8/4.7, jusqu'à 2,5× le débit de tokens de sortie, tarif premium) est une fonctionnalité **API** — indisponible par le chemin abonnement. Or 98 % du temps d'un tour est de la génération, donc c'est le seul levier de latence en dehors d'une réduction du volume de sortie. Conséquence : la facturation API cesse d'être une question purement contractuelle, elle conditionne aussi la **latence** atteignable en UX conversationnelle. Corollaire mesuré : sur abonnement, `total_cost_usd` est un équivalent-API et non un débit — ce qui est consommé est du quota (cf. Q-0013). |
| Q-0011 | Outillage du scoutisme de données : quel accès aux portails externes (opendata.swiss, geo.admin.ch, OFS…) depuis une session, sous quelles contraintes (réseau, licences, P8), et quel format pour les fiches « pressenties » du catalogue ? | 2026-07-27 | Architecture | Issu d'ADR-0007. Le scoutisme « de tête » (connaissances universelles du LLM) fonctionne sans outillage dès la v1 ; l'outillage est cible v2. **Recherche 2026-07-29** : `lust0yixiong/geodata-atlas` = catalogue open data **curé par un pipeline d'agents LLM** (découverte + vérif URL + dédup + trace de preuve) = blueprint méthodologique du scoutisme ; standards de fiche à considérer : **Frictionless Data Package** + **Croissant**. Cf. veille §7. |
| Q-0012 | Ratchet vers `pyright strict` : introduire des couches typées autour de la stack maison (duckdb, fastmcp, claude-agent-sdk, pyyaml) pour révoquer la déviation `standard` ? | 2026-07-28 | Technique / qualité | Issu d'ADR-0008. `src` est en `typeCheckingMode="standard"` faute de stubs tiers ; le retour à `strict` passe par des wrappers typés (ou l'arrivée de stubs), puis un ADR qui `Supersedes` ADR-0008. |
| Q-0013 | Modèle LLM par rôle d'agent : quel modèle pour chaque rôle (analyste/critique = le plus capable ; greffier/facilitateur/curateur = plus léger) ? | 2026-07-28 | Architecture / coût | Banc brique #1 (5 runs/modèle) : **Sonnet passe** l'oracle (5/5, 5/5, faux=0), **Haiku échoue** en *complétude* (concentration 3/5, mais faux=0 — il n'hallucine pas). L'oracle ne teste que le **plancher** (honnêteté + évidences), pas la finesse d'analyse → l'analyste garde le modèle le plus fort (`opus` épinglé dans le runner). Le banc sert de plancher pour les rôles mécaniques. Optimisation coût par-rôle = v2+. **Raffinement (cadrage brique #7, 2026-08-02)** : le « modèle par rôle » se concrétise en **profil de rôle** (ADR-0009) — la config (charte + outils + contrat de sortie + terminaison) vit dans le profil, pas dans le code ; le choix du modèle LLM devient un champ du profil. Recoupe Q-0021. **#7c (v0.10.0, 2026-08-04)** : matérialisé — le modèle passe à `run_agent(profile, prompt, *, model=…)` ; le curateur tourne `opus`. Optimisation coût par-rôle = v2+. **Raffinement (brique #9, v0.11.0, 2026-08-05)** : le coût par rôle devient **mesurable**, plus seulement estimable — `metrics_report` donne coût, durées et tokens par tour pour n'importe quelle trace, y compris **rétroactivement**. Premier chiffre réel : **0,42 $ pour UN tour** de curateur sur `opus` (9 939 tokens de sortie), là où le run analyste complet de la brique #5 coûtait 0,19 $. L'arbitrage « quel modèle pour quel rôle » cesse d'être théorique ; il attend surtout que la **sortie** soit ramenée à une taille raisonnable (cf. Q-0022, Q-0023) — optimiser le modèle avant la verbosité serait traiter le symptôme. **Raffinement (gate #8 corrigé, 2026-08-06)** : premier chiffre sur une **séance complète** — **1,8033 $ pour 5 tours** de curation sur `opus` (734 s bout en bout, 460 s d'API), contre 0,19 $ pour un run analyste complet. Tarifs Opus 4.x vérifiés en doc live : entrée 5 $/M, sortie 25 $/M, **écriture** de cache 6,25 $/M (TTL 5 min) ou 10 $/M (1 h), **lecture** de cache 0,50 $/M. **Deux réserves de lecture** : (a) recalculer le total depuis les compteurs visibles donne ~1,40 $ — l'écart est cohérent avec des tokens de raisonnement facturés que `output_tokens` ne reflète pas entièrement (hypothèse, pas mesure) ; (b) le run tourne sur l'**abonnement** (OAuth, Q-0010), donc `total_cost_usd` est un **équivalent-API** et non un débit — ce qui est réellement consommé est du **quota**. L'arbitrage « quel modèle pour quel rôle » attend toujours que la sortie soit ramenée à une taille raisonnable (Q-0023) et que l'entrée cesse d'être re-créée (Q-0024). **Le temps est désormais MODÉLISÉ (2026-08-07)**, ce qui change l'angle de cette question. Mesuré sur deux séances : `temps ≈ tokens générés ÷ 72`, le hors-API pesant 3,4 s sur 961. La décomposition tombe à la seconde près — **la fiche 56 à 70 % du temps, la prose 18 à 23 %, le raisonnement 12 à 21 %**. Conséquence : le bon arbitrage par rôle n'est plus « quel modèle » mais **combien un rôle a le droit d'écrire**, et le seul levier de débit hors réduction du volume est le **Fast mode**, indisponible par le chemin abonnement (Q-0010). Un levier concret jamais éprouvé : `thinking=False` vaut **21 % du temps** sur le curateur, et `demo_curator` passe `thinking=True` sans que ce choix ait jamais été discuté. |
| Q-0014 | Stratégie de sortie structurée de l'agent (verdict) : garder l'extraction regex tolérante, ou passer à une sortie structurée robuste (JSON mode forcé côté Claude, ou balise délimitante) ? | 2026-07-29 | Technique / robustesse | Surgi de la revue spec↔code brique #1 : `agent/verdict.py::parse_verdict` prend le **premier** tableau JSON du texte — fragile si l'agent émet un tableau d'exemples *puis* le verdict. Tolérable en brique #1 (la charte impose « UNIQUEMENT un tableau JSON »), à trancher **avant que la charte ne gagne en complexité**. Déjà noté comme minor dans l'execution recap ; élevé en décision ici. Cf. journal 2026-07-29. **Raffinement (brique #8, 2026-08-05)** : côté **curateur**, le contrat est passé à **prose libre + un bloc JSON de métadonnées** (`fiche_draft`, `proposes_completion`), le message étant le texte hors blocs ; `_BLOCK` accepte toute langue de fence (une fence illustrative ne déplace plus le bloc de métadonnées) et les replis tolérants sont conservés. **La justification initiale est RÉFUTÉE** : l'hypothèse « une string JSON échappée pousse à la compression » est fausse — le transcript gold-standard, dense et excellent, a été produit **avec la prose dans le JSON**. Le format n'était pas le frein. **Justification retenue à la place : la robustesse.** À ~10 000 tokens de sortie par tour, un bloc JSON malformé fait perdre **tout le message** ; en prose libre, le repli conserve le texte et seules les métadonnées sont perdues (tour dégradé, pas tour perdu). À ne pas raconter autrement dans les sessions futures. L'**analyste** garde son contrat d'origine (`parse_verdict`, extraction du premier tableau JSON) — la question reste ouverte pour lui. |
| Q-0015 | Politique de **curation de la fiche** : comment le curateur produit une fiche *correcte, sûre et non-fuyante* ? | 2026-07-30 | Architecture / sécurité | Extraite de Q-0004 (qui était surchargée) ; regroupe trois facettes de la responsabilité du curateur au moment de rédiger la fiche. **(a) Inférence de type** : distinguer un numérique *continu* d'un *code* numérique déguisé (cardinalité/n faible, `uniqueness`≈1, pics discrets, sentinelle hors plage) — décide le bon profil ; **interview humaine** si ambigu (n° BFS, code postal ; cf. MCP Elicitation Q-0008). Émergé brique #2 : `vitesse_limite_kmh` typée `categorical` à dessein pour faire ressortir `999` — en `numeric` la sentinelle se diluerait. **(b) Sensibilité des sorties agrégées** : un agrégat n'est pas anodin — `max(salaire)`/`max(âge)` ré-identifient, CA annualisé = secret, **cellules rares** du `top_k` / décomptes < *k* = violation de **k-anonymat**. Prévoir une politique par colonne au-delà de l'allowlist binaire : *exclure* / *avertir* / *restreindre* (pas de min/max sur colonne sensible) / *seuil de k-anonymat* (supprimer valeurs/cellules sous *k*). Lié à P8 + instruction confidentialité org. **(c) Hygiène anti-spoiler** : `describe` renvoie la fiche **verbatim** à l'agent → aucune *intention de fixture / méta-donnée de test* ne doit y figurer (piège brique #2 : un champ `note: "…révèle 999…"` spoilait l'anomalie que l'oracle doit voir découverte seule) ; l'intention design reste **dev-side** (commentaire de `build_fixture.py`). La fiche ne porte que de la connaissance métier légitime (`sens`, `piege`, `srid`…). **Facette (a) livrée (#7b, v0.9.0, 2026-08-02)** : `mcp_server/profiling_raw.py::infer_type` infère un **type candidat** par colonne d'un dataset **non-fiché** (heuristique : SQL type → categorical/numeric/temporal/spatial ; numérique de **faible cardinalité** `≤ 25` = **code déguisé**, cf. `999`). Démo réelle sur OFROU brut : `AccidentUID` card = nb de lignes → signalé catégoriel mais crie « identifiant » = exactement le candidat que le curateur corrige. Le type est un **candidat, jamais une vérité** — le curateur/humain valide. Restent (b) sensibilité/k-anon et (c) hygiène anti-spoiler, au curateur **#7c**. **#7c (v0.10.0, 2026-08-04)** : (a) inférence de type **consommée** par le curateur ; **(c) anti-spoiler gravé dans la charte** (« aucune méta-donnée de test / indice d'intention ») ; (b) sensibilité/k-anonymat portées par le **gate de validation humaine**, pas encore outillées. **Facette (d) neuve, arbitrée le 2026-08-07 — la DENSITÉ de la fiche.** Point de doctrine posé : la fiche a **deux lecteurs aux besoins opposés**. Le **LLM** la relit **verbatim à chaque analyse future** via `describe` — il paie la taxe de prose à chaque fois ; l'**humain** ne la lit qu'**une fois**, à la validation, et il valide surtout sur le résumé des pièges du dernier tour. On écrivait pour l'humain et on facturait au LLM. **Décision d'Alex : fiche dense, le résumé de validation restant la porte d'entrée humaine** — écrire deux projections serait une cathédrale pour un lecteur qui passe une fois. Mesures qui l'appuient : la fiche v3 fait 26 278 car. de contenu (médiane **461 car./colonne**, contre 132 en #8) ; **20 des 37 entrées sont des libellés traduits** documentés séparément, soit 5 746 car. pour redire vingt fois la même chose, réductibles à une entrée transversale de ~300 car. ; et une densification sans perte d'aucun fait donne **−62 à −66 %** sur les colonnes réelles. Cible : ~8 100 car., soit **−177 s par séance**. |
| Q-0016 | **Choix et validation de l'exposition** d'un dataset pour le test de concentration (et les futurs tests normalisés) : comment la déterminer, qui la fournit, comment la valider ? | 2026-07-30 | Architecture / méthodologie | Émergée du Q&A brique #3. L'exposition est le **dénominateur** qui transforme un comptage brut en taux : la réponse à *« qu'est-ce qui gonfle mécaniquement le comptage, indépendamment du phénomène qui m'intéresse ? »* (accidents→trafic/km réseau ; ventes→surface ; cas→population ; défauts→taille de lot). C'est une **question métier**, pas technique : le code ne peut pas la deviner. Décisions posées en brique #3 : (a) l'exposition est **déclarée dans la fiche** (convention `exposures`), jamais codée en dur (agnosticité) ; (b) sans exposition déclarée → **modèle nul uniforme** (affirmation plus faible, signalée par `exposure_model` dans la sortie) ; (c) l'outil **explicite** l'hypothèse, il ne l'invente pas. Restent ouverts : **qui/comment** élicite l'exposition (rôle curateur + interview humaine → recoupe Q-0008 et Q-0015) ; **plusieurs expositions candidates** (trafic vs km réseau vs population) → **test de robustesse** (la conclusion tient-elle quel que soit le dénominateur raisonnable ? — extension future, cousine du test anti-MAUP) ; **exposition réelle** (couche trafic TMJA / km réseau externe) en remplacement de l'exposition synthétique de la fixture. Recoupe Q-0009 (rigueur, mécanisme), Q-0015 (curation de la fiche), Q-0008 (élicitation / MCP Elicitation). **Raffinement (cadrage brique #5, 2026-08-01)** : la validation réelle exercera l'exposition = **population cantonale (BFS)**, proxy **grossier assumé** (≠ trafic ; TJM national trop lacunaire, cf. `research/2026-08-01-donnees-suisses-et-quarto.md`) avec la réserve **gravée dans la fiche réelle**. Le trafic réel comme dénominateur reste le travail futur. **Raffinement (brique #5 impl, 2026-08-01)** : la décision (b) « à défaut → modèle nul **uniforme** » est **remise en cause**. Pour des unités de tailles inégales (cantons…), l'uniforme suppose l'équiprobabilité — presque toujours faux — et **ré-introduit la confusion volume↔excès** que l'outil existe pour éliminer (il flaggerait mécaniquement la plus grosse unité ; sur la fixture, il « verrait » ZH sur-concentré, la mauvaise réponse que l'exposition déclarée réfute). Nouvelle orientation à implémenter en **slice dédiée** (correction de `concentration.py`, livré en brique #3 — **hors brique #5**) : l'exposition devient un **choix conscient** — (i) table déclarée → nul pondéré ; (ii) `uniform` **opt-in explicite** dans la fiche (le curateur *affirme* l'échangeabilité : dé, semaines, machines identiques) → nul uniforme assumé ; (iii) ni l'un ni l'autre → **abstention** (« sur-concentration non évaluable sans exposition »), pas un verdict trompeur. Aligne (c) « l'outil explicite l'hypothèse, il ne l'invente pas » + honnêteté P6. N'impacte pas brique #5 (run réel = exposition population déclarée). **Raffinement (brique #6, design+plan advisor SHIP, 2026-08-01)** : l'orientation « **abstention par défaut** » est **portée en implémentation** — `concentration.py` : sans exposition déclarée **ni** opt-in uniforme explicite → abstention (fin du défaut uniforme silencieux) ; opt-in `{uniform: true}` dans la fiche = échangeabilité assumée. Brique #6 introduit aussi une **exposition spatiale réelle** — population résidente/hectare (BFS STATPOP) agrégée en cellules H3 — comme dénominateur du test de **robustesse d'échelle**, via une **fiche STATPOP auto-descriptive référencée** (règle « donnée consommée = fiche curée » ; cf. mémoire `consumed-data-needs-curated-fiche`). Réserve gravée : au grain hexagone la **population ≠ trafic** mord davantage (corridors de transit peu peuplés → cellules « sans population », **exclues du test et reportées à part**) ; l'**exposition trafic** réelle reste le travail futur. Exéc. en session fraîche. **Livré (brique #6, v0.7.0, 2026-08-02)** : abstention par défaut **en production** dans `concentration.py` (opt-in uniforme explicite `{uniform: true}` requis) ; exposition **spatiale réelle** (STATPOP hectare→H3) livrée comme dénominateur du test de robustesse d'échelle, via **fiche référencée** (convention : clé d'exposition = **colonne de jointure** du dataset). Restent ouverts : exposition **trafic** réelle ; **plusieurs expositions par colonne de jointure** (extension `dict`→liste + sélection un-vs-tous = brique future robustesse-multi-expositions). **Confirmé (#7c, v0.10.0, 2026-08-04)** : les `exposures` relèvent de la **découverte, pas de la curation d'ingestion** (le curateur produit le bloc `columns` seul ; l'exposition, question métier, émerge à la découverte) → **brique découverte dédiée**. |
| Q-0017 | Emplacement et cycle de vie des ADR : la séparation `Proposed` (spec) → déplacement vers `Accepted` (impl) vaut-elle sa friction, ou faut-il regrouper les ADR d'un projet en un seul endroit quel que soit leur statut ? | 2026-07-31 | Doctrine / méthodologie | Émergée en promouvant ADR-0001 + ADR-0008 (2026-07-31). Constat : (a) le déplacement à l'acceptation exige une **transformation** (résolution des liens spec→impl, neutralisation des refs journal) — d'où une friction réelle jamais formalisée ; (b) tant que la majorité des ADR restent `Proposed`, la vue des décisions du projet est **fragmentée sur deux repos** ; (c) en mono-développeur, `spec/` et `src/` sont toujours clonés ensemble → l'autonomie de l'impl est aujourd'hui théorique. Contre-argument doctrinal (garder la séparation) : le livrable ne doit montrer que des décisions **arrêtées** ; le spec est éphémère → les décisions durables survivent mieux dans l'impl ; l'overview (qui vit dans l'impl) et les ADR forment un tout. **Point clé** : toucher à cette règle = modifier la doctrine 3 entités (ADR-0002/0003 méthodo) → **hors périmètre d'une session projet**, à arbitrer par une *revue d'usage de standards* depuis `methods/spec`. **Recoupe Q-0013 (méta-spec `methods/spec`)** : mécanisme de promotion `Proposed → Accepted` exercé mais jamais formalisé. **#7c (v0.10.0, 2026-08-04)** : #7 complet (socle #7a + profil brut #7b + curateur #7c) rend **ADR-0009 mûr pour `Accepted`** ; laissé `Proposed` au session-end 2026-08-04 car le flip exerce cette friction spec→impl non résolue (+ slice conversationnelle Q-0022 ouverte). |
| Q-0018 | **Autonomie de la réfutation** : un *backlog d'hypothèses* piloté par la réfutation (vue kanban) où des agents en loop exécutent la **batterie d'invalidateurs** (tous les outils qui peuvent ou pourront invalider une hypothèse) et l'humain arbitre la promotion — vaut-il d'être construit en v2+, et sous quelle forme ? | 2026-08-01 | Architecture / méthodologie | Émergée d'une exploration (2026-08-01). L'idée initiale — « des agents autonomes qui s'auto-alimentent en hypothèses pour découvrir des insights, visualisés sur un kanban qui se remplit tout seul » — a été **retournée** : auto-*générer* des insights est trivial, **commoditisé** (prior art massif : AI Scientist/Sakana + v2 tree-search, Robin/Nature, AI Co-Scientist Google, Hypothesis-Hunting evolving networks ; côté kanban d'agents : `agent-kanban`, `Hermes`, `Vibe Kanban` (Bloop fermé début 2026 → signal de commoditisation), `claude-code-kanban`, `Fusion`, `gastown`) **et toxique ici** : un essaim qui pond des hypothèses en boucle = *garden of forking paths* **industrialisé**, reproduit à l'échelle machine « cheaply and at scale » par le PNAS 2026 *« Many AI analysts, one dataset »* — exactement le mode d'échec que le projet combat (Q-0009). **Renversement (la valeur)** : l'autonomie sert à **tuer** les hypothèses, pas à les pondre. Le kanban devient un *backlog de réfutation* où une carte n'atteint « promue » qu'après **survie à tous les invalidateurs applicables** **et** promotion humaine filtrée (Q-0004/Q-0015). **La réfutation ≠ modèles nuls** : le modèle nul (concentration_test, Q-0016) n'est **qu'un** invalidateur d'une **batterie ouverte et extensible** — statistiques (LISA/Gi\*, anti-MAUP `esda.smaup`, NKDE réseau, corrections multi-tests FDR, E-value/confondeur caché, Empirical Bayes/RTM, réfutation causale `dowhy` refuters — cf. Q-0009) **et non-statistiques** (contrôle qualité/pièges de la donnée via la fiche : sentinelles, hors-emprise ; contre-hypothèses concurrentes ; échantillon drill-down borné, cf. Q-0008 ; cohérence avec connaissances externes). Le « ou pourront » est structurant : chaque nouvel outil de rigueur livré s'enfiche comme invalidateur supplémentaire. Les agents en loop exécutent cette part **mécanique/déterministe** ; l'humain métier alimente les cartes en amont (la vraie question) et arbitre en aval. Colonnes candidates : `hypothèse → réfutation en cours → survit (candidat fiche) → réfutée/abstention → promue (MR humain)`. **Substrat déjà présent** : la trace du greffier (`sessions`+`nodes`, provenance append-only) est un *board latent* — une carte = projection d'un sous-arbre de trace ; la fiche est le confluent des faits-donnée promus ; le graphe par-analyse porte les insights-sur-le-monde. Le board serait une **vue**, pas un organe neuf (test de plus-value : l'auto-génération, un LLM générique la fait déjà seul → WOW-theatre ; la réfutation disciplinée + provenance + abstention, non). **Outillage** : pas `/loop` (re-trigger naïf sans état) mais `Workflow` (orchestration déterministe, fan-out) + store persistant = le kanban ; subagents sans état entre invocations. **Bloquée par l'amont** : massivement v2+ — exige le **multi-tours réel** (palier C, non livré) et surtout **Q-0002** (vrai destinataire) ; automatiser la découverte avant qu'un seul insight n'intéresse une personne réelle = pattern Henry/Algiz sous stéroïdes. **Petit pas honnête** si un jour exercée : d'abord **une** vraie session multi-tours, observer si la trace se projette naturellement en cartes (si oui, board quasi-gratuit ; si non, organe de trop). Recoupe Q-0002, Q-0003, Q-0004, Q-0009, Q-0015, Q-0016 ; palier C (recap brique #5). |
| Q-0019 | **Dérive d'isolation P3 du runner analyste** : les méta-outils récents du CLI Claude Code (`ToolSearch`, `TaskCreate`, `Cron*`…) sont **hors `disallowed_tools`** et donc atteignables par l'analyste — comment restaurer l'isolation P2/P3 proprement ? | 2026-08-02 | Sécurité / architecture | Émergée du gate démo brique #6. Constat : la trace montre l'agent **appelant `ToolSearch`** → `permission_mode="bypassPermissions"` + `allowed_tools` **n'empêche pas** l'appel d'outils hors-liste ; `disallowed_tools` est la **vraie barrière** (docstring du runner) et ne couvre que les **anciens** built-ins (Bash/Read/Web/Skill…), pas les méta-outils introduits par l'update CLI (`2.1.187`). Le `TaskCreate` remontant dans les résultats de recherche est le **symptôme**. Environnemental, **pré-existant** (bruit `ToolSearch` déjà noté au recap brique #5 / v0.6.0). Mécanisme observé : double `ToolSearch` — forme `select:<noms nus de la charte>` **échoue** (les outils sont enregistrés `mcp__intreepid__…`), repli sur la forme **mots-clés** réussit et fournit les noms complets. À cadrer en **slice sécurité dédiée** : recenser les méta-outils du CLI courant ; comprendre le mécanisme d'**outils différés** du SDK (différé vs directement appelable) ; verrouiller l'isolation (durcir `disallowed_tools` et/ou config SDK) avec **test de non-régression**. Recoupe la posture P2/P3. **Raffinement (ADR-0009, 2026-08-02)** : l'isolation est désormais **portée par le profil de rôle** (`build_options` : allowlist + `disallowed_tools`) via l'orchestrateur générique → le durcissement des méta-outils s'y branche **une fois** pour tous les rôles ; le garde-test d'isolation (`test_runner_options.py`) reste le filet. **#7c (v0.10.0, 2026-08-04)** : l'isolation du **curateur** est portée par son profil (`build_options` : allowlist `profile_raw` seul + `_DISALLOWED` miroir + `strict_mcp_config`), avec test d'isolation dédié ; la dérive méta-outils CLI reste non traitée (durcissement à brancher une fois pour tous les profils). **Cause identifiée (gate #8 corrigé, 2026-08-06)** : les 4 appels `ToolSearch` du run ne sont pas de la curiosité de l'agent, c'est **notre faute**. La charte nomme l'outil `profile_raw` alors qu'il est enregistré `mcp__intreepid__profile_raw` : l'agent tente `select:profile_raw` (échec sur le nom nu), se rabat sur une recherche par mots-clés, récupère le nom complet, puis appelle l'outil — deux fois, une par appel de `profile_raw`. **Correctif à un mot** : nommer l'identifiant complet dans la charte, ce qui supprime les 4 appels. Cela retire le **symptôme**, pas le trou : `ToolSearch` reste hors de `disallowed_tools`. **Cause réelle établie (gate #10, 2026-08-07)** : le correctif a été appliqué et **mesuré**, et le diagnostic du 06/08 était **incomplet**. Le nom complet a bien supprimé le repli par mots-clés — 2 appels `ToolSearch` par usage d'outil sont devenus **1** (4 appels pour 4 usages, contre 4 pour 2 le matin). Mais `ToolSearch` reste appelé **une fois par outil**, parce que les outils MCP sont **différés** dans ce CLI : leur schéma doit être chargé avant appel. Le nom n'était pas la cause, c'était l'**aggravation** — et **aucun renommage ne peut supprimer le reste**. Conséquence de méthode : le critère de gate « zéro `ToolSearch` » avait été bâti sur cette prémisse fausse et ne pouvait pas être atteint. |
| Q-0020 | **Interrogation descriptive directe (à travers la fiche) + accompagnement maïeutique** : exposer la requête simple comme point d'entrée d'une analyse, et la couche qui *reformule* la question — quand, comment, pour qui ? | 2026-08-02 | Architecture / produit | Émergée d'une discussion doctrinale (2026-08-02) qui a affûté la **mission** (gravée en overview v0.3, §1 « Contrat d'interrogation »). Le contrat : *interrogation libre, réponse qualifiée jusqu'au bout de ce qu'on peut/ne peut pas savoir, aide à reformuler.* Facettes de la couche produit à cadrer : **(a)** exposer la **requête descriptive simple** comme *point d'entrée* légitime d'une analyse (« nombre total ? », « accidents moto 11h-13h hiver ? ») — non-triviale car protégée par la **fiche** (sentinelle `999` ≠ 999 km/h ; unités/SRID ; **k-anonymat** des cellules rares, cf. Q-0015) ; interroger *à travers la fiche* ≠ interroger DuckDB nu → passe le test de plus-value v1. **(b)** La couche **maïeutique** (le différenciateur *génératif*) : répondre à la question posée, puis **révéler la question qu'il fallait poser** et proposer le chemin (« … et cette exposition = trafic nocturne ; on la cherche ? » → rejoint le scoutisme **Q-0011**). **(c)** **Régimes de coût** : mode *requête* léger vs mode *analyse* complet — si canal court, il passe **quand même** par la fiche + politique de sortie (sinon = DuckDB MCP nu, différenciateur perdu). **(d)** **Trois garde-fous non négociables** : *répondre d'abord, reformuler ensuite* (le fait brut dû immédiatement, la reformulation est une offre) ; *proposer sans imposer* (l'humain reste pilote) ; *émerger de la fiche, pas d'un script* (l'écart « question posée ↔ question juste » se **déduit** de l'exposition déclarée/manquante — pas de reformulation câblée, cf. mémoire `no-hard-coded-scenarios` + dette charte Q-0004). **Distinction posée** : deux natures de « ce que la réponse n'est pas » — *épistémique* (P6, on dit toujours tout, jamais une limite) vs *protection* (P8/I-5/Q-0015, non-divulgation, orthogonale à la vérité). **Posture, pas moteur** : rien à construire aujourd'hui ; l'embryon existe (refus causal motivé brique #5) ; **bloquée en amont par Q-0002** (se valide avec un vrai destinataire, pas sur le papier). Recoupe Q-0002, Q-0004, Q-0008 (MCP Elicitation), Q-0011 (scoutisme), Q-0015 (protection). |
| Q-0021 | **Modèle de configuration / séparation des agents** : « la charte est la config du rôle ; un agent = **profil** (charte + allowlist d'outils MCP + contrat de sortie + critère de terminaison), pas seulement un `.md` ». Comment généraliser proprement au-delà de 2 rôles ? | 2026-08-02 | Architecture / méthodologie | Émergée du cadrage brique #7 (Fork B). **Matérialisée partiellement par ADR-0009** : orchestrateur générique + 2 profils (analyste one-shot, curateur multi-tours) ; l'analyse conversationnelle partage le profil-boucle du curateur (différée, Q-0002). Reste ouvert : la généralisation aux 6 rôles de l'overview §3 (critique, candide, greffier, facilitateur) — valider **à l'usage** que « agent = profil » tient sans framework spéculatif (anti-cathédrale). Recoupe Q-0013 (modèle LLM par rôle) et Q-0019 (isolation portée par le profil). **Exécuté (#7a, v0.8.0, 2026-08-02)** : l'orchestrateur générique `run_agent(profile, …)` + `Profile` (dataclass minimale : options/parse/on_result) sont **en production** ; l'analyste est le **1ᵉʳ profil réel** (`analyst_profile.py`, one-shot), `run_analysis` réduit à un wrapper mince (non-régression). Le greffier a été rendu **agnostique du rôle** (primitive `record_nodes`, cf. Q-0004) : un rôle n'a plus à toucher le socle pour émettre ses nœuds. **Reste ouvert** : le curateur (#7c) sera le **2ᵉ profil** (multi-tours) = test décisif ; généralisation aux 6 rôles toujours à valider à l'usage. **Résolu à 90 % (#7c, v0.10.0, 2026-08-04)** : le curateur est le **2ᵉ profil réel** (multi-tours) — le *test décisif* de « agent = profil » est **passé** (une seule boucle `run_agent`, l'analyste one-shot strictement préservé). Reste la généralisation aux 6 rôles (à valider à l'usage). Structure : **package par rôle** adopté (`agent/curator/` fait ; `agent/analyst/` = follow-up). |

| Q-0024 | **Quelle primitive du SDK pour un rôle conversationnel, et pourquoi le cache d'entrée ne tient-il pas en multi-tours ?** | 2026-08-06 | Architecture / coût | Émergée du gate de la brique #8 corrigée. **Mesuré sur un run réel de 5 tours** : les lectures de cache **plafonnent à 14 137 tokens dès le tour 3** (58 059 puis 53 236 aux tours 1-2) tandis que les **créations croissent** (10 014 → 16 122 → 23 327). L'historique grandissant est donc **re-créé** en cache à chaque tour au lieu d'être étendu. **Cause immédiate** : `curator/profile.py::_serialize` sérialise tout l'historique en **un seul message utilisateur** ; le cache étant un préfixe d'octets et cette chaîne différant à chaque tour, le point de cache s'arrête à la charte (les 14 137 constants). **Cause racine** : la docstring de `claude_agent_sdk.query` déclare « **Stateless**: each query is independent, no conversation state » et renvoie explicitement vers `ClaudeSDKClient` pour « Interactive conversations with follow-ups / Chat applications or **REPL-like interfaces** / Long-running sessions with state ». Nous avons construit un REPL sur la primitive one-shot. **Conséquence budgétaire** : l'écriture de cache coûte 1,25× l'entrée (6,25 $/M sur Opus 4.x) contre 0,1× en lecture (0,50 $/M) — la re-création est donc payée au tarif le plus cher de l'entrée. **Ce qui reste à trancher** : passer la boucle multi-tours à `ClaudeSDKClient` (touche le cœur d'ADR-0009 et exige de re-prouver la non-régression de l'analyste one-shot), ou rester sur `query()` en passant l'historique comme **séquence de messages structurés** (`AsyncIterable[dict]`, accepté par la signature) plutôt qu'en chaîne concaténée. Mérite son propre cycle brainstorm/design/plan, pas un ajout de fin de slice. Recoupe ADR-0009 (boucle générique), Q-0023 (coût de sortie), Q-0013 (coût par rôle), Q-0010 (le Fast mode, seul autre levier de latence, est API-only). **Deux faits nouveaux (gate #10, 2026-08-07)**. (a) **La question s'est améliorée sans qu'on la traite** : les lectures de cache **ne plafonnent plus** (31 469 → 62 044 → 67 638 → 76 827, contre un plafond à 14 137 dès le tour 3 le matin). L'historique est donc étendu et non re-créé ; la chute du dernier tour s'explique par le **TTL de 5 minutes**. Effet de bord non anticipé du brouillon incrémental — un préfixe de prompt plus stable — à confirmer sur un second run. (b) **Recadrage de la portée** : cette question est un sujet de **coût**, pas de **temps**. Mesuré sur deux séances complètes, le **hors-API pèse 3,4 s sur 961** — l'entrée, le cache, les appels d'outil et le démarrage sont négligeables en temps. Traiter Q-0024 ne fera gagner **aucune seconde** ; à ne pas prioriser tant que l'objectif est le temps et la maîtrise du contenu. |
| Q-0025 | **Le mur de l'attention humaine à l'échelle** : à 300 couches, combien de décisions humaines une curation a-t-elle le droit de coûter, et comment les réutiliser d'un jeu à l'autre ? | 2026-08-07 | Produit / méthodologie | Émergée du recadrage d'Alex (2026-08-07) : l'objectif du curateur n'est ni l'économie ni le prix, c'est **la maîtrise des appels et de leur contenu**, et **le temps** — parce que la cible est de documenter des **centaines** de couches, où douze minutes par jeu est impossible. **Position posée, et elle ferme une fausse piste** : il ne doit exister **aucun plafond** du nombre de questions. Cent questions sur un jeu sont légitimes si cent jugements exigent une autorité que le profil n'a pas ; le nombre est une **conséquence** du critère de seuil, jamais un réglage. La charte porte d'ailleurs un **plafond déguisé** à retirer (« quelques questions structurantes suffisent, même pour plusieurs dizaines de colonnes »), tandis que la première moitié de la phrase (« jamais une passe colonne par colonne ») reste — c'est elle le vrai garde-fou contre le comportement robotique de v0.10.0. **Le problème que cela ouvre** : sans plafond, 300 couches peuvent coûter **30 000 décisions humaines**. Une fois les leviers de génération appliqués (~250 s/jeu), l'attention humaine devient le **seul** mur, et aucune optimisation du curateur ne l'entame. **Piste, non cadrée** : beaucoup de questions ne portent pas sur *ce* jeu mais sur les **conventions du producteur** — référentiel LV95, codes OFS millésimés, motif de sentinelle `at0`/`at00`, grain accident-vs-victime. Répondues une fois, elles valent pour **toutes les couches du même producteur**. Forme pressentie : une **mémoire de curation par producteur**, consultée avant de poser une question ; si la convention est déjà ratifiée ailleurs, l'agent la reprend et le signale au lieu de la redemander. Même mouvement que la fiche « confluent des faits-donnée » de Q-0004, appliqué aux **décisions de curation** au lieu des faits d'analyse. **Garde anti-cathédrale** : ne rien construire avant d'avoir un **second jeu du même producteur** ; le premier pas honnête est d'observer, sur la deuxième couche OFROU curée, combien de questions sont des redites de la première. Recoupe Q-0002 (destinataire réel), Q-0004, Q-0015, Q-0020. |

## Résolues

| ID | Question | Résolue | Pointeur de résolution |
|---|---|---|---|
| Q-0023 | Où vit la mémoire d'un agent multi-tours ? Quel canal porte ce qu'il a déjà établi, et sous quelle forme ? | 2026-08-07 | **Résolue — la mémoire vit dans l'application, sous forme de deltas fusionnés par clé.** Livrée en **v0.13.0** (`agent/curator/draft.py`) : l'agent n'émet plus que les colonnes tranchées ou corrigées depuis son tour précédent ; `merge_delta` accumule (dernière écriture gagne, `columns` entrée par entrée) ; `inventory_line` lui rend l'inventaire via `build_prompt` — **jamais** via la réponse humaine, que l'orchestrateur grave en `human_turn`. La valeur d'une colonne reste **opaque** : Python n'en connaît que la clé `columns`, les noms et `dataset`. **Zéro ligne d'orchestrateur** : la non-régression de l'analyste one-shot est structurelle. Le brouillon émis baisse de **36 %** (55 604 → 35 837 car.). **RÉSERVE MAJEURE, à ne pas raconter autrement : le bénéfice espéré ne s'est PAS matérialisé.** Le gate humain a **échoué** (2 critères sur 5) et le mécanisme est **neutre sur le temps** — les 65 s économisées sur la fiche ont été intégralement reprises par le raisonnement (+39 %) et la prose (+7 %) ; le coût a même monté de 14,5 %. La branche a été mergée sur décision d'Alex **non comme livrable mais comme prérequis** de la structure en deux appels à venir (sans le delta, la révision finale ré-émettrait la fiche entière). **La sous-question « quel volume de sortie un tour doit-il coûter » est reformulée** : l'objectif n'est ni le volume ni le coût mais le **temps**, désormais modélisé (cf. Q-0013). **Ce qui reste ouvert migre** : généralisation du patron aux autres rôles conversationnels → Q-0021 ; contrat et schéma de la fiche → Q-0004 ; densité → Q-0015 ; mur de l'attention humaine → Q-0025. Pointeurs : `slices/2026-08-07-01-brique-10-brouillon-incremental-execution.md`, `journal/2026-08-07-le-mecanisme-marche-le-temps-ne-bouge-pas.md`, `research/2026-08-06-brique10-gate-materiel.md` (§ 8 = plan de reprise), `src/CHANGELOG.md [0.13.0]`. |
| Q-0022 | Style conversationnel du curateur : rendre actif **par défaut** le niveau que seule une précision live de l'humain obtenait. | 2026-08-06 | **Résolue — le niveau est actif par défaut.** Gate humain du 06/08 sur l'OFROU brut (267 761 lignes, 36 colonnes) : le **premier tour** porte les cinq éléments attendus — objet, ancrage chiffré tiré du profil, enjeu rendu tangible par un mécanisme concret, options fermées avec « je ne sais pas » déclaré valide, penchant assumé avec son indice contraire — **sans aucune consigne de style** (les réponses de l'humain tiennent en `1a` / `2a` / `3b` / `4a` / `o`). Fiche 36 colonnes validée, et **deux trouvailles absentes de v0.10.0** : `AccidentSeverityCategory` ordinale à l'envers (`as1` = le plus grave) et absence totale de comptage de victimes. Le correctif décisif a été un **retrait** : charte de 158 à 138 lignes, une question par tour (comme le transcript de référence, qui n'en a demandé que 7 pour 36 colonnes), mémoire de l'agent rendue à son brouillon de fiche au lieu d'être versée dans le canal humain. **Réserve explicite, qui ne rouvre pas cette question** : le *calibrage* reste ouvert — questions 3 à 5 fois plus longues que la référence (2 130-3 367 caractères contre ~700), et 4 ratifications humaines au lieu de 7, donc plus de décisions solitaires. Ce calibrage vit dans **Q-0023** (volume de sortie) et dans la slice de tuning à venir ; le *mécanisme*, lui, est acquis. Pointeurs : `slices/2026-08-06-01-brique-8-correction-charte-execution.md`, `journal/2026-08-06-brique8-correction-et-mesure-arbitrable.md`, `src/CHANGELOG.md [0.12.0]`. |
| Q-0007 | Observabilité technique : le logging DuckDB maison suffit-il, ou Langfuse (auto-hébergé) en v2 ? | 2026-08-05 | **Le maison suffit** — tranché par la brique #9 (v0.11.0). La trace du greffier porte déjà tout : la fin de tour devient un **nœud horodaté** (`turn_result` : durées, coût, tokens), la durée d'un appel d'outil s'obtient par **appariement des horodatages** `tool_call`/`tool_result`, et `metrics_report` relit **n'importe quelle trace déjà écrite** — donc mesure **rétroactive**, sans organe supplémentaire ni dépendance. Preuve d'usage immédiate : le run curateur du 05/08 a chiffré 98 % de génération, 9 939 tokens de sortie, 0,42 $/tour et un cache d'entrée déjà pleinement exploité (43 781 tokens relus, 6 facturés) — ce qui a tranché D3 de la brique #8. **Réserve** : la mesure est par tour, jamais par outil (le SDK ne ventile pas le coût) ; un besoin futur d'agrégation cross-sessions ou de dashboard rouvrirait le sujet — il serait alors traité comme une **nouvelle** question, pas comme une reprise de celle-ci. Pointeurs : `slices/2026-08-05-01-brique-9-mesure-cout-et-temps-execution.md`, `journal/2026-08-05-brique8-gate-rate-et-mesure.md` §4-5. **Correction apportée le 2026-08-06, sans rouvrir la résolution** : l'affirmation « cache d'entrée déjà pleinement exploité » ci-dessus est **fausse en multi-tours** — elle avait été mesurée sur un run d'**un seul tour**. Sur un run de 5 tours, les lectures de cache plafonnent tandis que les créations croissent : l'historique est re-créé à chaque tour. Cf. **Q-0024**. La résolution elle-même tient (la mesure maison suffit) ; c'est l'un de ses exemples d'usage qui était trop optimiste. |

## Différées

| ID | Question | Apparue | Raison / cible |
|---|---|---|---|
| _aucune pour l'instant_ | | | |
