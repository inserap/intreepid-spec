# Questions ouvertes — spec

> Liste vivante des items non résolus ou différés pour le projet `intreepid`. Les questions touchant la doctrine partagée (`standards/`) sont résolues ici, même si elles produisent un commit doctrinal côté standards.
> Mise à jour pendant les rituels session-end (bins c. additions, d. résolutions).
> Les IDs `Q-NNNN` sont **immuables** : ne jamais réutiliser un Q-NNNN même après résolution.

## Comment utiliser ce fichier

- **Ajouter** un item quand une session fait émerger une vraie ambiguïté non résolvable en séance.
- **Résoudre** un item quand une session livre une réponse (ADR, entrée journal, slice execution).
- **Raffiner** un item quand sa formulation évolue.

## Ouvertes

| ID | Question | Apparue | Catégorie | Notes |
|---|---|---|---|---|
| Q-0001 | Nom définitif du projet ? | 2026-07-26 | Produit / naming | Nom de code actuel : `intreepid`. Candidats pressentis : *Semantree*, *Semantrek*. Anciens candidats (Ramure, Sylva, Dendrite, Arolle) abandonnés. Critère : raconter l'arbre *et* l'enracinement territorial/sémantique. |
| Q-0002 | Dataset pilote et question métier réelle pour la v1 ? | 2026-07-26 | Produit / périmètre | Cas accidents (OFROU, open data, couches geo.admin.ch) pressenti mais non confirmé ; aucun destinataire métier identifié à ce jour. **Raffinement (cadrage brique #5, 2026-08-01)** : la brique #5 est cadrée comme **banc de validation de la v1 sur la vraie donnée OFROU** — substitut *contrôlable* à cette question en attendant un vrai destinataire ; elle **ne la résout pas** (ni question métier réelle ni destinataire), mais exerce le système de bout en bout sur du réel. **Validée (brique #5 impl, v0.6.0, 2026-08-01)** : la validation end-to-end sur le réel a **réussi** — posture P6 confirmée sur donnée propre (aucune anomalie hallucinée), volume≠excès démontré (BE sur-concentré / ZH gros comptage expliqué par l'exposition), refus causal motivé, run $0,19. **Q-0002 reste ouverte** : substitut contrôlé, pas un vrai destinataire métier. |
| Q-0003 | Granularité des nœuds de l'arbre : quels critères pour que le greffier décide ce qui mérite un nœud ? | 2026-07-26 | Technique | Pari technique central du projet. À calibrer sur sessions réelles, pas sur le papier. **Raffinement (recherche greffier, 2026-07-30)** : la littérature fournit des heuristiques publiées (H1–H8, cf. `research/2026-07-30-greffier-provenance.md` §4) — grain-cible = **Action sémantique** (Gotz & Zhou 2009 : ni notes manuelles ni events bruts) ; un nœud naît à (a) résultat interprétable d'une Action ou (b) changement d'hypothèse/sujet ; Events bruts conservés mais **repliés** sous l'Action (fin en stockage, agrégé en lecture) ; coalescence des actions consécutives même-artefact ; vocabulaire de `kind` **petit et fermé** (le texte-libre optionnel échoue — Goyal CHI 2013) ; indexation **par artefact, pas par chronologie** (Verdant ; Kery CHI 2018, empirique). Décision brique #4 : capture au **grain événement** sans tri à chaud ; la projection « Action » est une **lecture future** — la calibration sur session réelle reste l'issue de cette question. **Capture livrée (2026-07-31, v0.5.0)** : le grain événement est en production (thinking summarized inclus — 2 nœuds 💭 observés sur le run de démo) ; la calibration « Action sémantique » attend une vraie session multi-tours (Q-0002). |
| Q-0004 | Schéma du catalogue sémantique et modèle de données de l'arbre/trace (avec attribution et empreintes spatiales) ? | 2026-07-26 | Architecture | Prochaine étape n°3 de l'architecture — conditionne tout le reste. **Raffinements (revue externe 2026-07-26)** : (a) en faire le *premier contrat exécutable* (schéma YAML catalogue + modèle d'événement de trace) avant tout code ; (b) empreinte de reproductibilité d'un insight = hash dataset + version catalogue + SQL + params + versions DuckDB/extensions + modèle + version prompt/charte (le « SQL rejouable » seul ne suffit pas si Parquet/catalogue évoluent) ; (c) invariants géospatiaux du dataset, pas champs descriptifs : SRID (EPSG:2056), reprojection, unités de distance, validité géométrique, version H3 ; (d) contrat MCP read-only à durcir en invariants *testés* : bornes sur sorties + quotas temps/mémoire + whitelist datasets/relations + audit par appel + interdiction écritures indirectes (COPY/INSTALL/extensions). **Raffinement biographie (session 2026-07-26)** : distinguer dans le modèle deux natures de résultat — *fait intrinsèque à la donnée* (vrai quelle que soit la question → promu à la **biographie/fiche**, canal **cross-analyse** partagé) vs *insight sur le monde* (spécifique à une analyse → **graphe de connaissances**, canal par-analyse). La fiche est le confluent où les faits-donnée découverts par **n'importe quelle** analyse s'accumulent (ex. une quirk de la couche routes trouvée via l'analyse accidents sert à l'analyse entretien). Le schéma doit prévoir : (i) un champ de faits-donnée promus avec **provenance** (session + requête, P4), (ii) le critère de promotion « vrai et utile quelle que soit la question », (iii) la promotion **filtrée** curateur→MR humain (garde anti-gonflement de la fiche, cousin de Q-0003 / critique NotebookLM n°3), (iv) rappel : une découverte fortuite reste hypothèse jusqu'à survie au critique/modèle nul (Q-0009) avant promotion. **Raffinement frontière charte↔fiche (brique #1, 2026-07-28)** : à graver — la *posture* (honnêteté fait/hypothèse/refusé, jamais de lignes brutes) reste **générique** dans la charte des agents ; la *connaissance per-dataset* (pièges, sens des colonnes, codes sentinelles) vit dans la **fiche**, lue via `describe` au runtime. C'est ce partage qui permet « traiter n'importe quelles données » sans gonfler le prompt. La charte de la brique #1 porte du **surajustement de scénario** (exemple `999`, instruction « concentration → hypothèse » tunée pour l'oracle) → à extraire vers la fiche / des exemples neutres lors de la généralisation **sur un 2ᵉ dataset réel** (ne se fera pas tout seul — dette à solder délibérément). **Recherche + revue 2026-07-29** : le split *Skills = flux cognitif ÷ MCP = calcul* de `kevics1/geo-perception` est la même frontière charte↔fiche sous un autre nom ; le *contexte-extension YAML* de `stuzero/pg-mcp-server` et la *clé BFS* de `malkreide/swisstopo-mcp` sont des patrons concrets pour la fiche `describe` ; la revue spec↔code a re-confirmé que `agent/charter.md` porte encore les exemples tunés (`999`, concentration→hypothèse). Cf. `research/2026-07-29-etat-de-lart-github.md`. **Note (2026-07-30)** : les responsabilités de *curation de la fiche* (inférence de type, sensibilité/k-anonymat, hygiène anti-spoiler) sont **extraites en Q-0015** pour ne pas surcharger cette question ; Q-0004 reste focalisée sur le **schéma** catalogue/fiche + modèle de trace. *(Grooming à prévoir : sortir aussi les fils « biographie/fait-vs-insight » et « frontière charte↔fiche » vers des questions dédiées — cf. follow-up journal 2026-07-30.)* **Raffinement modèle de trace (brique #4, 2026-07-30)** : premier **contrat exécutable** posé au design du greffier — tables DuckDB immuables `sessions` (statuts `open/closed/aborted`) + `nodes(kind, parent_id, content/meta JSON)`, append-only, scellées à la clôture ; nommage des relations en vocabulaire **PROV-DM allégé** (verdict recherche : emprunter Entity/Activity/Agent + `wasInformedBy`/`wasDerivedFrom`/`wasAttributedTo`, **écarter** la stack RDF/PROV-O/PROV-JSON — RETEX : « PROV a gagné les specs, perdu les SDK » ; conversion a posteriori via `trungdong/prov` si un consommateur externe l'exige) ; extension pressentie par **facets** (patron OpenLineage `_producer`/`_schemaURL`, schémas côté fiche — cohérent frontière charte↔fiche). Ne clôt pas Q-0004 (mono-analyste quasi-linéaire ; DAG multi-parent, artefacts versionnés façon Verdant et facets outillés = extensions futures). **Corrections à la veille 2026-07-29** : PROV-AGENT a bien un repo vérifiable (`ORNL/flowcept`, MIT, actif) ; les conventions OTel GenAI ont migré vers `open-telemetry/semantic-conventions-genai`. Cf. `research/2026-07-30-greffier-provenance.md`. **Contrat livré + clarification session≠analyse (brique #4 impl, 2026-07-31, v0.5.0)** : le contrat `sessions`+`nodes` est **en production** (append-only vérifié par tests sur les 3 états `closed`/`aborted`/`open`-crashée). Vocabulaire gravé au Q&A du gate : **session = épisode de capture** (un run de l'analyste) ≠ **analyse = fil multi-sessions** (jours/mois, reprises, branches en attente) ; la reprise d'une analyse se fait **par référence** à l'histoire (`wasInformedBy`/`wasDerivedFrom`, jamais par réouverture d'une session scellée — sémantique git). Le lien inter-sessions est le prochain étage du schéma. V2-list revue finale : nœud `verdict_raw` quand `parse_verdict` échoue (le texte brut non parsé est perdu hors `aborted_reason`) ; `render()` n'affiche pas `SessionTrace.meta` (coûts/num_turns). **Raffinement (cadrage brique #5, 2026-08-01)** : contrat de **fiche auto-descriptive** conçu — la fiche porte `data:` + `exposures` en chemins **relatifs-à-la-fiche**, le serveur MCP est pointé par `INTREEPID_FICHE` (une fiche = point d'entrée unique, `dataset` = nom de table) ; **notebook Quarto** posé comme projection lisible/rejouable de la trace (`to_quarto` pur, templating figé, callouts fait/hypothèse/refusé). Conçus au **palier A**, en **attente d'impl** (design/plan en vol). **Shippé (brique #5, v0.6.0, 2026-08-01)** : `to_quarto` (générateur agnostique, golden-testé) + fiche auto-descriptive (`data:`+`exposures` relatifs, serveur pointé par `INTREEPID_FICHE`) sont **en production**. Restent ouverts : liens inter-sessions (`wasInformedBy`), DAG, distillation, rappel MCP. |
| Q-0005 | Réutilisation concrète du substrat Henry : les Plans/Traits sont-ils repris tels quels, adaptés, ou seulement comme inspiration de format ? | 2026-07-26 | Architecture | ADR-0005 pose l'absorption dans le principe ; le degré reste à trancher au moment du squelette. |
| Q-0006 | Empreintes spatiales floues : seuils de la représentation dégradée (emprise exacte → communes → canton) ? | 2026-07-26 | Technique | À trancher lors de l'implémentation du couplage arbre-carte (v2). |
| Q-0007 | Observabilité technique : le logging DuckDB maison suffit-il, ou Langfuse (auto-hébergé) en v2 ? | 2026-07-26 | Technique | Décision différée — une ligne de risque, pas une couche. |
| Q-0008 | Injection de prompt via colonnes textuelles des données : quelles données du périmètre contiennent du texte tiers, et quel traitement ? | 2026-07-26 | Sécurité | Risque identifié lors de la revue écosystème ; à qualifier avec le dataset pilote. **Piste (revue externe 2026-07-26)** : le modèle « le LLM ne voit pas les lignes » réduit le risque mais valeurs catégorielles, échantillons et métadonnées restent du contenu tiers non fiable. Définir un type `untrusted_data` dans le protocole de rendu des outils MCP : encodage structuré, quotas de taille, neutralisation des instructions, séparation visuelle stricte dans le contexte agent. **Recherche 2026-07-29** : `kyle-gao/SecuringGISAgent` outille le durcissement de prompts + l'évaluation adversariale d'agents géo (rare) ; `diogocosme/geodados-mcp-server` illustre l'**MCP Elicitation** (le serveur *demande* en cas d'ambiguïté) = parade au mode d'échec « résolution silencieuse de l'ambiguïté » mesuré par `hsd2514/geoagenteval`. Cf. veille §3/§5. **Raffinement échantillon-drill-down (brique #2, 2026-07-30)** : P2 autorise explicitement les *échantillons* — donc, sur détection d'anomalie (sentinelle, points hors emprise), tirer un **échantillon borné, pseudonymisé, traité `untrusted_data`** pour comprendre/compléter l'origine est légitime et renforce le curateur ; cadrer les garde-fous (taille bornée, k-anonymat cf. Q-0015, neutralisation d'instructions, read-only, servi comme résultat d'outil et non ingestion de table). |
| Q-0009 | Protocole de preuve statistique spatiale : quels invariants de rigueur imposer avant de retenir un pattern ? | 2026-07-26 | Technique / statistique | Émergé de la revue externe 2026-07-26, complète la mention MAUP + modèles nuls de l'overview. À couvrir : stationnarité, autocorrélation spatiale, découpage temporel, prévention de fuite spatiale (spatial leakage) en validation ML. Se matérialisera dans la charte des agents (critique) et le protocole des modèles nuls. **Recherche 2026-07-29 (outils concrets)** : `pysal/esda` = modèles nuls spatiaux par permutation (`significance.py`) + **test MAUP dédié** (`smaup.py`) ; `SamComber/spacv` = validation croisée spatiale anti-leakage (SLOO) ; `statsmodels.stats.multitest` = corrections multi-tests (garden of forking paths ; BSD vs Pingouin GPL) ; `py-why/dowhy` refuters → outil MCP `refute_insight`. Cf. veille §5. **Brique #3 (2026-07-30) — mécanisme livré** : le premier modèle nul est en production (`concentration_test` : permutation multinomiale conditionnée à l'exposition, pseudo-p `(M+1)/(R+1)`, écart de Poisson standardisé, 2 volets `most_concentrated`/`highest_raw_count`). Restent ouverts, par ordre de valeur pressenti (veille §5) : autocorrélation locale Gi\*/LISA (`esda`, sur maille H3), test MAUP multi-résolution H3 (`esda.Smaup`), méthodes réseau NKDE (biais planaire des accidents routiers), corrections multi-tests FDR (`statsmodels`), E-value (confondeur caché), Empirical Bayes/RTM (régression vers la moyenne), réfutation causale (`dowhy`). Le **choix de l'exposition** (le dénominateur qui conditionne le nul) est extrait en **Q-0016**. **Raffinement (deep-research H3 + brique #6, 2026-08-01)** : la veille `research/2026-08-01-h3-agregation-anti-maup-reseau.md` outille la roadmap (Gi\*/LISA via `esda.G_Local` + null par permutation ; correction multi-tests **FDR obligatoire** — Caldas de Castro & Singer 2006 ; `pygeoda lisa_fdr`/`lisa_bo`) et pose un **verdict planaire** : pour un phénomène **contraint au réseau** (accidents), le binning aréal H3 est biaisé — méthode **réseau** (NKDE, Xie & Yan 2008) plus appropriée, à garder pour une brique future (**hors** brique #6). **Brique #6 (design+plan advisor SHIP, exéc. à venir)** livre le premier volet : robustesse **multi-résolution** d'une concentration (survit-elle au changement de maille H3 ?), null ∝ exposition, garde-fous (autocorrélation sous le null, non-emboîtement ~14 %, cellules non équi-aires). Gi\*/FDR **local** restent différés. |
| Q-0010 | Modèle d'authentification et de facturation des agents (Claude Agent SDK) : dev vs produit déployé ? | 2026-07-26 | Architecture / contractuel | **Établi** (doc officielle Anthropic) : le Claude Agent SDK s'authentifie par clé API `ANTHROPIC_API_KEY` (ou cloud provider Bedrock/Vertex/Foundry/AWS), **pas** par abonnement ; l'usage de l'abonnement / claude.ai login pour un **produit tiers** est interdit *sauf approbation préalable d'Anthropic* ; un produit déployé relève des Commercial Terms of Service. **Dev / prototypage local par l'auteur = usage interactif permis** → la phase squelette peut tourner sur le compte perso à quota. **Ouvert** : négocier avec l'account team enterprise (a) l'exception « previously approved » pour faire tourner les agents produit sur le plan, (b) le tarif/volume API du backend produit. À élever en ADR quand le modèle de facturation produit se décide. Sources : docs Agent SDK (code.claude.com), Commercial Terms. **Dev résolu en pratique (brique #1, 2026-07-28)** : l'Agent SDK tourne sur l'**abonnement** via l'auth Claude Code stockée (`claude setup-token` → `CLAUDE_CODE_OAUTH_TOKEN`) ; garde runtime dans le runner : refuser de tourner si `ANTHROPIC_API_KEY` est définie (elle masque l'OAuth et facturerait l'API). **Reste ouvert** : facturation du produit déployé. |
| Q-0011 | Outillage du scoutisme de données : quel accès aux portails externes (opendata.swiss, geo.admin.ch, OFS…) depuis une session, sous quelles contraintes (réseau, licences, P8), et quel format pour les fiches « pressenties » du catalogue ? | 2026-07-27 | Architecture | Issu d'ADR-0007. Le scoutisme « de tête » (connaissances universelles du LLM) fonctionne sans outillage dès la v1 ; l'outillage est cible v2. **Recherche 2026-07-29** : `lust0yixiong/geodata-atlas` = catalogue open data **curé par un pipeline d'agents LLM** (découverte + vérif URL + dédup + trace de preuve) = blueprint méthodologique du scoutisme ; standards de fiche à considérer : **Frictionless Data Package** + **Croissant**. Cf. veille §7. |
| Q-0012 | Ratchet vers `pyright strict` : introduire des couches typées autour de la stack maison (duckdb, fastmcp, claude-agent-sdk, pyyaml) pour révoquer la déviation `standard` ? | 2026-07-28 | Technique / qualité | Issu d'ADR-0008. `src` est en `typeCheckingMode="standard"` faute de stubs tiers ; le retour à `strict` passe par des wrappers typés (ou l'arrivée de stubs), puis un ADR qui `Supersedes` ADR-0008. |
| Q-0013 | Modèle LLM par rôle d'agent : quel modèle pour chaque rôle (analyste/critique = le plus capable ; greffier/facilitateur/curateur = plus léger) ? | 2026-07-28 | Architecture / coût | Banc brique #1 (5 runs/modèle) : **Sonnet passe** l'oracle (5/5, 5/5, faux=0), **Haiku échoue** en *complétude* (concentration 3/5, mais faux=0 — il n'hallucine pas). L'oracle ne teste que le **plancher** (honnêteté + évidences), pas la finesse d'analyse → l'analyste garde le modèle le plus fort (`opus` épinglé dans le runner). Le banc sert de plancher pour les rôles mécaniques. Optimisation coût par-rôle = v2+. |
| Q-0014 | Stratégie de sortie structurée de l'agent (verdict) : garder l'extraction regex tolérante, ou passer à une sortie structurée robuste (JSON mode forcé côté Claude, ou balise délimitante) ? | 2026-07-29 | Technique / robustesse | Surgi de la revue spec↔code brique #1 : `agent/verdict.py::parse_verdict` prend le **premier** tableau JSON du texte — fragile si l'agent émet un tableau d'exemples *puis* le verdict. Tolérable en brique #1 (la charte impose « UNIQUEMENT un tableau JSON »), à trancher **avant que la charte ne gagne en complexité**. Déjà noté comme minor dans l'execution recap ; élevé en décision ici. Cf. journal 2026-07-29. |
| Q-0015 | Politique de **curation de la fiche** : comment le curateur produit une fiche *correcte, sûre et non-fuyante* ? | 2026-07-30 | Architecture / sécurité | Extraite de Q-0004 (qui était surchargée) ; regroupe trois facettes de la responsabilité du curateur au moment de rédiger la fiche. **(a) Inférence de type** : distinguer un numérique *continu* d'un *code* numérique déguisé (cardinalité/n faible, `uniqueness`≈1, pics discrets, sentinelle hors plage) — décide le bon profil ; **interview humaine** si ambigu (n° BFS, code postal ; cf. MCP Elicitation Q-0008). Émergé brique #2 : `vitesse_limite_kmh` typée `categorical` à dessein pour faire ressortir `999` — en `numeric` la sentinelle se diluerait. **(b) Sensibilité des sorties agrégées** : un agrégat n'est pas anodin — `max(salaire)`/`max(âge)` ré-identifient, CA annualisé = secret, **cellules rares** du `top_k` / décomptes < *k* = violation de **k-anonymat**. Prévoir une politique par colonne au-delà de l'allowlist binaire : *exclure* / *avertir* / *restreindre* (pas de min/max sur colonne sensible) / *seuil de k-anonymat* (supprimer valeurs/cellules sous *k*). Lié à P8 + instruction confidentialité org. **(c) Hygiène anti-spoiler** : `describe` renvoie la fiche **verbatim** à l'agent → aucune *intention de fixture / méta-donnée de test* ne doit y figurer (piège brique #2 : un champ `note: "…révèle 999…"` spoilait l'anomalie que l'oracle doit voir découverte seule) ; l'intention design reste **dev-side** (commentaire de `build_fixture.py`). La fiche ne porte que de la connaissance métier légitime (`sens`, `piege`, `srid`…). |
| Q-0016 | **Choix et validation de l'exposition** d'un dataset pour le test de concentration (et les futurs tests normalisés) : comment la déterminer, qui la fournit, comment la valider ? | 2026-07-30 | Architecture / méthodologie | Émergée du Q&A brique #3. L'exposition est le **dénominateur** qui transforme un comptage brut en taux : la réponse à *« qu'est-ce qui gonfle mécaniquement le comptage, indépendamment du phénomène qui m'intéresse ? »* (accidents→trafic/km réseau ; ventes→surface ; cas→population ; défauts→taille de lot). C'est une **question métier**, pas technique : le code ne peut pas la deviner. Décisions posées en brique #3 : (a) l'exposition est **déclarée dans la fiche** (convention `exposures`), jamais codée en dur (agnosticité) ; (b) sans exposition déclarée → **modèle nul uniforme** (affirmation plus faible, signalée par `exposure_model` dans la sortie) ; (c) l'outil **explicite** l'hypothèse, il ne l'invente pas. Restent ouverts : **qui/comment** élicite l'exposition (rôle curateur + interview humaine → recoupe Q-0008 et Q-0015) ; **plusieurs expositions candidates** (trafic vs km réseau vs population) → **test de robustesse** (la conclusion tient-elle quel que soit le dénominateur raisonnable ? — extension future, cousine du test anti-MAUP) ; **exposition réelle** (couche trafic TMJA / km réseau externe) en remplacement de l'exposition synthétique de la fixture. Recoupe Q-0009 (rigueur, mécanisme), Q-0015 (curation de la fiche), Q-0008 (élicitation / MCP Elicitation). **Raffinement (cadrage brique #5, 2026-08-01)** : la validation réelle exercera l'exposition = **population cantonale (BFS)**, proxy **grossier assumé** (≠ trafic ; TJM national trop lacunaire, cf. `research/2026-08-01-donnees-suisses-et-quarto.md`) avec la réserve **gravée dans la fiche réelle**. Le trafic réel comme dénominateur reste le travail futur. **Raffinement (brique #5 impl, 2026-08-01)** : la décision (b) « à défaut → modèle nul **uniforme** » est **remise en cause**. Pour des unités de tailles inégales (cantons…), l'uniforme suppose l'équiprobabilité — presque toujours faux — et **ré-introduit la confusion volume↔excès** que l'outil existe pour éliminer (il flaggerait mécaniquement la plus grosse unité ; sur la fixture, il « verrait » ZH sur-concentré, la mauvaise réponse que l'exposition déclarée réfute). Nouvelle orientation à implémenter en **slice dédiée** (correction de `concentration.py`, livré en brique #3 — **hors brique #5**) : l'exposition devient un **choix conscient** — (i) table déclarée → nul pondéré ; (ii) `uniform` **opt-in explicite** dans la fiche (le curateur *affirme* l'échangeabilité : dé, semaines, machines identiques) → nul uniforme assumé ; (iii) ni l'un ni l'autre → **abstention** (« sur-concentration non évaluable sans exposition »), pas un verdict trompeur. Aligne (c) « l'outil explicite l'hypothèse, il ne l'invente pas » + honnêteté P6. N'impacte pas brique #5 (run réel = exposition population déclarée). **Raffinement (brique #6, design+plan advisor SHIP, 2026-08-01)** : l'orientation « **abstention par défaut** » est **portée en implémentation** — `concentration.py` : sans exposition déclarée **ni** opt-in uniforme explicite → abstention (fin du défaut uniforme silencieux) ; opt-in `{uniform: true}` dans la fiche = échangeabilité assumée. Brique #6 introduit aussi une **exposition spatiale réelle** — population résidente/hectare (BFS STATPOP) agrégée en cellules H3 — comme dénominateur du test de **robustesse d'échelle**, via une **fiche STATPOP auto-descriptive référencée** (règle « donnée consommée = fiche curée » ; cf. mémoire `consumed-data-needs-curated-fiche`). Réserve gravée : au grain hexagone la **population ≠ trafic** mord davantage (corridors de transit peu peuplés → cellules « sans population », **exclues du test et reportées à part**) ; l'**exposition trafic** réelle reste le travail futur. Exéc. en session fraîche. |
| Q-0017 | Emplacement et cycle de vie des ADR : la séparation `Proposed` (spec) → déplacement vers `Accepted` (impl) vaut-elle sa friction, ou faut-il regrouper les ADR d'un projet en un seul endroit quel que soit leur statut ? | 2026-07-31 | Doctrine / méthodologie | Émergée en promouvant ADR-0001 + ADR-0008 (2026-07-31). Constat : (a) le déplacement à l'acceptation exige une **transformation** (résolution des liens spec→impl, neutralisation des refs journal) — d'où une friction réelle jamais formalisée ; (b) tant que la majorité des ADR restent `Proposed`, la vue des décisions du projet est **fragmentée sur deux repos** ; (c) en mono-développeur, `spec/` et `src/` sont toujours clonés ensemble → l'autonomie de l'impl est aujourd'hui théorique. Contre-argument doctrinal (garder la séparation) : le livrable ne doit montrer que des décisions **arrêtées** ; le spec est éphémère → les décisions durables survivent mieux dans l'impl ; l'overview (qui vit dans l'impl) et les ADR forment un tout. **Point clé** : toucher à cette règle = modifier la doctrine 3 entités (ADR-0002/0003 méthodo) → **hors périmètre d'une session projet**, à arbitrer par une *revue d'usage de standards* depuis `methods/spec`. **Recoupe Q-0013 (méta-spec `methods/spec`)** : mécanisme de promotion `Proposed → Accepted` exercé mais jamais formalisé. |
| Q-0018 | **Autonomie de la réfutation** : un *backlog d'hypothèses* piloté par la réfutation (vue kanban) où des agents en loop exécutent la **batterie d'invalidateurs** (tous les outils qui peuvent ou pourront invalider une hypothèse) et l'humain arbitre la promotion — vaut-il d'être construit en v2+, et sous quelle forme ? | 2026-08-01 | Architecture / méthodologie | Émergée d'une exploration (2026-08-01). L'idée initiale — « des agents autonomes qui s'auto-alimentent en hypothèses pour découvrir des insights, visualisés sur un kanban qui se remplit tout seul » — a été **retournée** : auto-*générer* des insights est trivial, **commoditisé** (prior art massif : AI Scientist/Sakana + v2 tree-search, Robin/Nature, AI Co-Scientist Google, Hypothesis-Hunting evolving networks ; côté kanban d'agents : `agent-kanban`, `Hermes`, `Vibe Kanban` (Bloop fermé début 2026 → signal de commoditisation), `claude-code-kanban`, `Fusion`, `gastown`) **et toxique ici** : un essaim qui pond des hypothèses en boucle = *garden of forking paths* **industrialisé**, reproduit à l'échelle machine « cheaply and at scale » par le PNAS 2026 *« Many AI analysts, one dataset »* — exactement le mode d'échec que le projet combat (Q-0009). **Renversement (la valeur)** : l'autonomie sert à **tuer** les hypothèses, pas à les pondre. Le kanban devient un *backlog de réfutation* où une carte n'atteint « promue » qu'après **survie à tous les invalidateurs applicables** **et** promotion humaine filtrée (Q-0004/Q-0015). **La réfutation ≠ modèles nuls** : le modèle nul (concentration_test, Q-0016) n'est **qu'un** invalidateur d'une **batterie ouverte et extensible** — statistiques (LISA/Gi\*, anti-MAUP `esda.smaup`, NKDE réseau, corrections multi-tests FDR, E-value/confondeur caché, Empirical Bayes/RTM, réfutation causale `dowhy` refuters — cf. Q-0009) **et non-statistiques** (contrôle qualité/pièges de la donnée via la fiche : sentinelles, hors-emprise ; contre-hypothèses concurrentes ; échantillon drill-down borné, cf. Q-0008 ; cohérence avec connaissances externes). Le « ou pourront » est structurant : chaque nouvel outil de rigueur livré s'enfiche comme invalidateur supplémentaire. Les agents en loop exécutent cette part **mécanique/déterministe** ; l'humain métier alimente les cartes en amont (la vraie question) et arbitre en aval. Colonnes candidates : `hypothèse → réfutation en cours → survit (candidat fiche) → réfutée/abstention → promue (MR humain)`. **Substrat déjà présent** : la trace du greffier (`sessions`+`nodes`, provenance append-only) est un *board latent* — une carte = projection d'un sous-arbre de trace ; la fiche est le confluent des faits-donnée promus ; le graphe par-analyse porte les insights-sur-le-monde. Le board serait une **vue**, pas un organe neuf (test de plus-value : l'auto-génération, un LLM générique la fait déjà seul → WOW-theatre ; la réfutation disciplinée + provenance + abstention, non). **Outillage** : pas `/loop` (re-trigger naïf sans état) mais `Workflow` (orchestration déterministe, fan-out) + store persistant = le kanban ; subagents sans état entre invocations. **Bloquée par l'amont** : massivement v2+ — exige le **multi-tours réel** (palier C, non livré) et surtout **Q-0002** (vrai destinataire) ; automatiser la découverte avant qu'un seul insight n'intéresse une personne réelle = pattern Henry/Algiz sous stéroïdes. **Petit pas honnête** si un jour exercée : d'abord **une** vraie session multi-tours, observer si la trace se projette naturellement en cartes (si oui, board quasi-gratuit ; si non, organe de trop). Recoupe Q-0002, Q-0003, Q-0004, Q-0009, Q-0015, Q-0016 ; palier C (recap brique #5). |

## Résolues

| ID | Question | Résolue | Pointeur de résolution |
|---|---|---|---|
| _aucune pour l'instant_ | | | |

## Différées

| ID | Question | Apparue | Raison / cible |
|---|---|---|---|
| _aucune pour l'instant_ | | | |
